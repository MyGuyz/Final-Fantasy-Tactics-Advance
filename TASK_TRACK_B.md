# Track B — ปลดล็อกสองเรื่องที่ค้างอยู่

งานสำหรับคนที่ 2 (ใช้ Claude Code) — อ่าน [CLAUDE.md](CLAUDE.md) และ [README.md](README.md) ก่อน

**เป้าหมาย:** ทำให้โปรเจกต์แปะข้อความไทยกลับเข้าเกมได้จริง

ตอนนี้มี 2 อย่างที่ขวางอยู่ ถ้าไม่แก้ **แปลเสร็จก็เอาเข้าเกมไม่ได้**

---

## สิ่งที่ต้องเตรียม

- ROM `Final Fantasy Tactics Advance (USA).gba` — SHA-1 `4ac05441f4de70a4ec3dd932116346c61b8783d9`
- Python 3 (ไม่ต้องลง dependency)
- **mGBA** — https://mgba.io (จำเป็นสำหรับงาน B1)

---

## งาน B1 — หาตำแหน่งฟอนต์ 🟡 เจอ ROM offset แล้ว เหลือถอด compression scheme

**ทำไมสำคัญ:** ถ้าไม่รู้ว่าฟอนต์อยู่ตรงไหน จะฝังฟอนต์ไทยไม่ได้เลย → แปลไปก็ขึ้นจอไม่ได้ **นี่คือ blocker ตัวจริงของโปรเจกต์**

**ทำอะไรไปแล้ว (อย่าทำซ้ำ):**
1. ไล่ `LZ77UnCompVram` (`0x14188C`) ครบทั้ง 5 จุดเรียกในเกมแล้ว เจอแค่โลโก้ไตติ้ล (`0x951588`) + พื้นหลัง 2 จุด + animation สไปรต์ 2 จุด — **ไม่มีฟอนต์**
2. ไล่ `LZ77UnCompWram` (`0x141890`) ด้วย static analysis — **แก้ตัวเลขเดิม:** สแกนจริงเจอ **19 จุดเรียก** ไม่ใช่ 58 ตามที่เคยเข้าใจผิด — ไล่ทุกจุดแล้วไม่เจอฟอนต์ (source pointer วิ่งผ่าน resource table แบบ indirect ตามรอยด้วย static analysis ไม่ได้)
3. Brute-force สแกนทั้ง ROM หา LZ77 มาตรฐาน (tag `0x10`) เจอ 3,371 บล็อก + LZSS ของเกมเอง (tag `0x01`) เจอ 615 บล็อก — decode หมดแล้วเทียบกับรูปทรงฟอนต์ **ไม่เจอสักบล็อก** (เจอกราฟิกอื่นเพียบ: ไอคอน, สไปรต์มอนสเตอร์, ดวงจันทร์ปฏิทิน, gradient)
4. **เปลี่ยนมาใช้ dynamic analysis — เจอแล้ว:** build mGBA แบบ headless (compile core + Python bindings เอง ไม่ต้อง GUI) รันเกมจริงพร้อมตั้ง memory watchpoint

**ผลลัพธ์ที่ยืนยันแล้ว (ดูรายละเอียดเต็มใน [README.md](README.md) หัวข้อ "ปัญหา 2"):**
- ฟอนต์ **ไม่ได้เป็น tile sheet บีบอัดแบบธรรมดา** — CPU **ประกอบขึ้นที่ runtime** ทีละไบต์ (ldrb/strb loop ไม่ผ่าน BIOS/DMA) — นี่คือสาเหตุที่ static scan ทุกแบบหาไม่เจอ
- ข้อมูลกลิฟต้นฉบับอยู่ที่ ROM **`0x08405EAA`** เป็นต้นไป (แต่น่าจะกลางตาราง ไม่ใช่จุดเริ่ม) ตัวควบคุมไบต์แรกที่ `0x08405EA9`
- **compression คือ LZSS-01 ตัวเดียวกับที่มีอยู่แล้วใน `tools/lzss01.py`!** ยืนยันด้วยการเทียบ copy-run 236 ครั้งกับสูตร opcode `0x40`-prefix ตรงครบ 236/236 — แค่เรียกโดยไม่มี header `0x01`+ขนาด 4 ไบต์ (รู้ขนาดปลายทางจากที่อื่น) และดูเหมือนมีหลาย pass ซ้อนกัน (outline+fill?) ยังต้องแยกวิเคราะห์ opcode ส่วน skip/back-reference เพิ่ม
- ฟังก์ชันที่เกี่ยวข้อง: `0x0800555C` (byte-copy loop) ← `0x0800543C` (วาดกลิฟ) ← `0x080CB800` (วาดข้อความ)
- ปลายทาง: WRAM `0x02003CB0` → VRAM `0x06009C00`/`0x06014800`/`0x06015800` — เห็นตัวอักษร "Aa" จริงตอน render

**เช็คลิสต์ว่าเสร็จ:**
- [x] รู้ ROM offset ของฟอนต์ (โดยประมาณ — `0x08405EAA`, ต้องหาจุดเริ่มตารางจริง)
- [ ] รู้รูปแบบ: ขนาดไทล์ (8×8 / 8×16?), bpp (1/2/4?), จำนวนกลิฟ — ยังต้องถอด RLE ให้ครบก่อน
- [x] รู้ว่าบีบอัดไหม — บีบอัดด้วย LZSS-01 เดิม (`tools/lzss01.py`) เรียกแบบไม่มี header, opcode "literal copy" ยืนยันแล้ว ส่วน skip/back-reference ยังไม่ครบ
- [ ] เขียน script ดัมพ์ฟอนต์ออกมาเป็น PNG ได้
- [ ] มีตาราง **ความกว้างตัวอักษร (VWF width table)** ด้วยหรือเปล่า — สำคัญมากตอนจัดข้อความไทย

### ขั้นต่อไป — ใช้ watchpoint ต่อ (วิธีที่ได้ผลแล้ว)

1. ตั้ง watchpoint แบบเดียวกันแต่ดักตั้งแต่ **ตัวอักษรตัวแรกที่วาดหลังบูตเครื่อง** (เร็วกว่าเดิมในเกม ไม่ต้องรอ mash ผ่านเมนู) จะได้ไล่ backward หา index/pointer table ที่แปลง character code → offset ในตารางฟอนต์ได้ง่ายกว่า
2. ไล่ดู log ของ copy-run/skip-run (มี pattern อยู่แล้วจาก log ตัวอย่าง) เพื่อถอด RLE scheme ให้ครบเป็นสูตรที่เขียนโค้ด decode ได้
3. วิธี build mGBA headless + วิธีตั้ง watchpoint เต็มๆ (พร้อมกับดักที่เจอระหว่างทาง) อยู่ใน [README.md](README.md) ท้ายหัวข้อ "ปัญหา 2" — ทำตามได้เลย ไม่ต้องเริ่มจากศูนย์

---

## งาน B2 — เก็บ 386 บรรทัดที่ถอดไม่ได้ 🟡

รายการครบพร้อม offset: [`data/categorized_text/_INCOMPLETE_needs_work.txt`](data/categorized_text/_INCOMPLETE_needs_work.txt)

**อาการ:** ถอดออกมาได้ครึ่ง ๆ เช่น `0x4b0231` ควรเป็น "Those Nightwalkers..." แต่ได้ `Those Nigwaersut`

**สาเหตุ:** มี back-reference อีกแบบที่**ไม่มี marker `0x32`** ให้จับ

### เบาะแสที่หามาได้แล้ว (เริ่มต่อจากนี้ได้เลย)

เอาสองสตริงที่ขึ้นต้นเหมือนกัน (`You can...`) มาเทียบไบต์ต่อไบต์:

| | A (`0x49b767`) | B (`0x49b407`) |
|---|---|---|
| ค่าที่เจอ | `A00B 9015 801F 8027 802B 802D` | `A00D 9017 8021 8029 802B 8031` |
| 12 บิตล่าง | 11, 21, 31, 39, 43, 45 | 13, 23, 33, 41, 43, 49 |
| 4 บิตบน | A, 9, 8, 8, 8, 8 | A, 9, 8, 8, 8, 8 |

B มีกลุ่มพิเศษแทรกเพิ่ม 1 ชุดตอนต้น แล้ว **ค่าทุกตัวหลังจากนั้นเพิ่มขึ้น +2 พอดีเป๊ะ** (11→13, 21→23, 31→33, 39→41)

**ตีความ:** 12 บิตล่าง = **offset สะสม** → ลายเซ็นของ back-reference แบบ LZ / 4 บิตบน (A, 9, 8) น่าจะเป็น length หรือ type

**ยังไม่รู้:** offset นับเป็นหน่วยอะไร (ไบต์ของ input? ของ output? ตัวอักษร?) และ 4 บิตบนแปลว่าอะไรแน่

**วิธีทดสอบ:** เขียน decoder ลองสมมติฐาน แล้ววัดว่าถอดได้เป็นภาษาอังกฤษอ่านรู้เรื่องกี่บรรทัดจาก 394 — ถ้าสมมติฐานถูก ตัวเลขจะกระโดดขึ้นชัดเจน

**ทดสอบแล้ว (ยังไม่ผ่าน):** ลองสมมติฐาน "marker 2 ไบต์ = back-reference ตำแหน่งสัมบูรณ์ใน output ของสตริงเดียวกัน ยาว = 4 บิตบน ตัวอักษร" กับข้อมูลทั้ง 394 บรรทัดจริง (อ่าน raw byte จาก ROM ตรงๆ ไม่ผ่าน placeholder) แล้ว **ถอดออกมาเป็น garbage หมด ไม่มีบรรทัดไหนอ่านรู้เรื่อง** สรุปว่าสมมติฐานนี้ผิดหรือยังไม่ครบ

**เบาะแสใหม่จากการตรวจ raw byte ตรงๆ:** เทียบ `0x49b767` กับ `0x49b407` แบบ hex เต็ม พบว่า marker แบบ `[XX 0][val]` (เช่น `A0 0B`) แทบทุกครั้งตามด้วย `40 XX` หรือ `44 XX` ทันที (เช่น `A0 0B 40 DB`, `90 15 40 E2`, `80 1F 40 D6`) — ชวนสงสัยว่า token ตัวจริงอาจ**ยาว 4 ไบต์** ไม่ใช่ 2 ไบต์อย่างที่เคยคิด ยังไม่ได้ทดสอบสมมติฐานนี้ (ดูรายละเอียด raw hex เต็มใน README.md "ปัญหา 1")

**ทำไมยังไปใช้ทางลัด RAM-dump ไม่ได้ตอนนี้:** เช็คแล้วพบว่าทั้ง 394 offset **ไม่มีสักรายการอยู่ในย่านที่เข้าถึงได้จากเมนูตอนบูตเครื่อง** — อยู่ใน `rumor_body` (256), `story_dialogue` (129), `battle_log` (9) ทั้งหมด ต้องเล่นผ่านสร้างตัวละคร + ภารกิจเปิดเกมก่อนถึงจะเข้าถึงได้ ต่างจากตอนหาฟอนต์ที่หน้าจอ New/Load Game โผลมาได้เร็วมากด้วยการ mash ปุ่มสุ่ม — วิธีนี้ต้องมี **save state ที่ผ่านภารกิจเปิดเกมแล้ว** หรือสคริปต์ปุ่มกดแบบ TAS ถึงจะใช้ mGBA watchpoint ดักได้แบบตอนหาฟอนต์

**รอบสาม (ล่าสุด) — ทดสอบสมมติฐาน 4-byte token กับข้อมูลจริงทั้ง 394 บรรทัด ด้วย dictionary คำศัพท์อังกฤษจริง:** ทุกสมมติฐานได้ word-hit rate ~32-35% เท่าๆ กันหมด (= noise level ไม่ใช่สัญญาณจริง) และลองค้นหา 32-bit pointer ที่ชี้ตรงไปยังสตริงตัวอย่าง 3 ตัว (`0x49b767`,`0x49b407`,`0x4b0231`) ทั่วทั้ง ROM — **ไม่เจอเลย** แปลว่าไม่ได้เก็บเป็น pointer table ตรงๆ (เหมือนฟอนต์ที่ static scan ก็หาไม่เจอเช่นกัน) รายละเอียดเต็มอยู่ใน README.md "ปัญหา 1"

**บทสรุป (ตอนนั้น):** static analysis 3 รอบยังไม่เจอสูตรถูกต้อง — สถานการณ์เหมือนฟอนต์ก่อนที่จะใช้ dynamic analysis จนเจอ **ต้องมี save state ผ่านภารกิจเปิดเกมก่อนถึงจะไล่ต่อด้วย watchpoint ได้จริง** ไม่แนะนำให้เดา static ต่อแบบไม่มีหลักฐานยืนยัน (เสี่ยงเข้าใจผิดซ้ำแบบบทเรียน control code `0x32` ใน CLAUDE.md)

**รอบสี่ — ผู้ใช้ส่ง save state จริงมาให้ (`.ss0`) ตรวจสอบแล้วตรงกับ ROM 100%:** เขียน watchpoint driver ที่โหลด save state ได้ ดักได้ 1 ใน 394 บรรทัด (`0x9c71f0`) ถอดออกมาตรงกับที่คาดเป๊ะ ยืนยัน charmap ถูก 100% — แต่บรรทัดนี้ติดใน incomplete เพราะ control code `0x40 0x21 XX` ที่ยังไม่รู้จัก (ไม่ใช่ mystery marker ตัวจริง) **แก้แล้ว: เพิ่มการรู้จัก `0x40 0x21 XX` ใน `dump_corpus3.py` ปิดได้ 8 บรรทัด (394→386)** ลองปล่อยเกมสำรวจต่อ (เดิน 4 ทิศ + ไล่เมนู pause ทุกตำแหน่ง) กว่า 50,000 เฟรมจาก save state เดิม ยังไม่เจอ mystery marker ตัวจริงเลย (ยังไม่มี save state ที่ใกล้ `rumor_body`/`battle_log` พอ) รายละเอียดเต็ม + บั๊ก r2-vs-r6 ที่เจอระหว่างทาง อยู่ใน README.md "ปัญหา 1" ส่วน "รอบสี่"

### ทางลัดที่การันตีผล (ต้องมี save state ก่อนถึงจะใช้ได้จริง — ตอนนี้มีเครื่องมือพร้อมแล้ว รอ save state ที่เหมาะสมกว่า)

ถ้าแกะ static ไม่แตก ใช้ mGBA ดัมพ์จาก RAM แทน — เกมถอดรหัสเองอยู่แล้วก่อนวาดจอ ไปอ่านผลลัพธ์ตรงนั้นเลย มีสคริปต์ Lua ให้แล้วใน [ROADMAP_TO_100.md](ROADMAP_TO_100.md) — **แต่ต้องมี save state ผ่านภารกิจเปิดเกมมาก่อน** (ดูเหตุผลด้านบน) วิธี build mGBA headless + ตั้ง watchpoint แบบ C driver โดยตรง (ไม่ผ่าน Lua, รองรับโหลด save state + dump WRAM แบบปลอดภัยแล้ว) มีบันทึกไว้ครบใน [README.md](README.md) ท้ายหัวข้อ "ปัญหา 2"

**เช็คลิสต์ว่าเสร็จ:**
- [ ] ถอด 386 บรรทัดที่เหลือได้ครบ (หรือได้จาก RAM dump) — ปิดไปแล้ว 8/394 บรรทัดด้วยการแก้ control code `0x40 0x21`
- [ ] อัปเดต `tools/dump_corpus3.py` ให้รองรับ (ถ้าแกะ static ได้)
- [ ] รัน `categorize3.py` + `gapreport.py` ใหม่ ให้ `_INCOMPLETE_needs_work.txt` เหลือ 0
- [ ] อัปเดตตัวเลขใน `README.md` และ `CATEGORIES_INDEX.md`

---

## งาน B3 — สำรวจ byte budget 🟢 ทำได้ถ้าว่าง

**ภาษาไทยยาวกว่าอังกฤษ** ต้องรู้ว่าแต่ละบล็อกข้อความมีที่ว่างแค่ไหนก่อนแปะกลับ

- แต่ละบล็อกมี byte budget เท่าไหร่ (ระยะถึงบล็อกถัดไป)
- มี padding / ที่ว่างให้ขยายไหม
- กล่องข้อความกว้างกี่พิกเซล ขึ้นได้กี่บรรทัด
- ถ้าที่ไม่พอ ย้ายข้อความไปท้าย ROM แล้วแก้ pointer ได้ไหม (ต้องหาตาราง pointer ก่อน)

---

## ลำดับที่แนะนำ

```
B1 (ฟอนต์)  ──► ถ้าเจอแล้ว บอกทีมทันที เพราะปลดล็อกทั้งโปรเจกต์
   │
   └─► ระหว่างรอ/ถ้าติด ─► B2 (386 บรรทัด) ─► B3 (byte budget)
```

**B1 สำคัญกว่า B2 มาก** — B2 ขาดไป 386 บรรทัดยังแปลส่วนอื่นไปก่อนได้ แต่ B1 ไม่เสร็จ = เอาอะไรเข้าเกมไม่ได้เลย

---

## กติกาการทำงานร่วมกัน

- **ไฟล์ที่ Track B แก้ได้:** `tools/`, `data/`, `FFTA_Recon_Analysis.md`, `TASK_TRACK_B.md`
- **อย่าแตะ** `translation/` (ของ Track A)
- `git pull --rebase` ก่อนเริ่มงานทุกครั้ง
- **ห้าม commit ROM** เช็ค `git status` ก่อน commit เสมอ
- เจออะไรใหม่ให้อัปเดต `README.md` / `CLAUDE.md` ด้วย คนอื่นจะได้ไม่ต้องค้นซ้ำ

## รายงานกลับ

เมื่อเสร็จแต่ละงาน อัปเดตเช็คลิสต์ข้างบนแล้ว commit — และเขียนสรุปสั้น ๆ ว่า**หาเจอได้ยังไง** (ไม่ใช่แค่ผลลัพธ์) จะช่วยคนถัดไปมาก
