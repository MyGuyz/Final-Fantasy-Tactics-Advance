# CLAUDE.md — บริบทโปรเจกต์สำหรับ Claude Code

ไฟล์นี้โหลดอัตโนมัติทุกครั้งที่เปิด Claude Code ใน repo นี้ อ่านก่อนเริ่มงาน

## โปรเจกต์นี้คืออะไร

แปล **Final Fantasy Tactics Advance (GBA, USA)** เป็นภาษาไทย
ตอนนี้อยู่เฟส "แกะข้อความ" — ยังไม่เริ่มแปล และยังฝังฟอนต์ไทยไม่ได้

อ่าน [README.md](README.md) ก่อนเสมอ มีสรุปสถานะและความรู้ทางเทคนิคทั้งหมด

## ROM — ต้องมีถึงจะทำงานได้

ไม่มีใน repo (ลิขสิทธิ์) ต้องหามาเองวางที่ root ชื่อ `Final Fantasy Tactics Advance (USA).gba`

**ต้องตรวจ SHA-1 ก่อนเริ่มงานเสมอ** — offset ทุกตัวใน repo ผูกกับเวอร์ชันนี้เท่านั้น ถ้าใช้เวอร์ชันอื่นทุกอย่างเพี้ยนหมด:
```
4ac05441f4de70a4ec3dd932116346c61b8783d9   (16,777,216 bytes)
```

หรือชี้ path เอง: `FFTA_ROM=/path/to/rom.gba python tools/dump_corpus3.py`

## กฎเหล็ก

1. **ห้าม commit ROM เด็ดขาด** — `.gitignore` กันไว้แล้ว แต่เช็ค `git status` ก่อน commit ทุกครั้ง
2. **repo นี้เป็น private โดยตั้งใจ** เพราะข้อความในนี้เป็นงานลิขสิทธิ์ Square Enix — อย่าเปลี่ยนเป็น public เอง
3. **อย่าแก้ไฟล์นอกสายงานตัวเอง** (ดูหัวข้อ "การแบ่งงาน") — ตอนนี้มี 2 คนทำพร้อมกัน ชนกันได้
4. **`git pull --rebase` ก่อนเริ่มงานทุกครั้ง**

## บทเรียนที่แพงที่สุดของโปรเจกต์นี้ — อ่านให้จบ

ตอนแรกเราข้าม control code `0x32` (ฝังสตริงบีบอัด LZSS) ไปเฉย ๆ ผลคือ**บทสนทนาเนื้อเรื่องหลักหายไปเกือบทั้งหมด** — แต่ coverage check ตอนนั้น**บอกว่าครบแล้ว** เพราะมันตรวจจาก pattern ที่เรานิยามขึ้นเอง พอ implement ตัวถอดจริงถึงกู้คืนมาได้ 1,491 บรรทัด

**สิ่งที่ต้องจำ:**
- coverage check ที่ตรวจจาก pattern ของตัวเอง **พิสูจน์ความครบถ้วนไม่ได้** มันแค่ยืนยันสมมติฐานตัวเอง
- ถ้าจะเคลมว่า "ครบแล้ว" ต้องมีหลักฐานจากนอกระบบตัวเอง เช่นเทียบกับข้อความที่เกมแสดงจริงผ่าน emulator
- เวลาเจอข้อความที่อ่านออกครึ่ง ๆ อย่าเหมาว่าเป็น noise — มันมักเป็นข้อความจริงที่ถอดไม่ถูก

## ความรู้ทางเทคนิคที่ verify แล้ว (ใช้ได้เลย ไม่ต้องพิสูจน์ซ้ำ)

**Charmap** (พิสูจน์ด้วยการถอดชื่อ Marche / Montblanc ตรงเป๊ะ):
```
multibyte:  A-Z = 0x80B0-0x80C9   a-z = 0x80CA-0x80E3
single-byte (สตริงขึ้นต้นด้วย 0x01):  A-Z = 0xB1-0xCA   a-z = 0xCB-0xE4
```

**Control codes** — `0x40` คือ prefix สากล:
```
0x00        จบสตริง
0x01        สลับเข้าโหมด single-byte
0x40 0x73   เว้นวรรค
0x40 0x6E   ขึ้นบรรทัดใหม่
0x40 0x70 + 0x40 0x63   จบหน้า/รอกดปุ่ม แล้วเคลียร์กล่อง
0x40 0x21 XX   ตัวเลือก portrait/mood ของผู้พูด (พารามิเตอร์ 1 ไบต์) มักอยู่ต้นแต่ละหน้าใหม่ — verify แล้วด้วย save state จริง (ดู README.md "ปัญหา 1" รอบสี่)
0x32        ฝังสตริงบีบอัด LZSS (0x32 XX + ขนาด 4 ไบต์ big-endian + data)
```

**เครื่องหมายวรรคตอนยังเป็นการเดา** (ยังไม่ verify แบบตัวอักษร) — ถ้าใครพิสูจน์ได้ให้อัปเดต README:
`{80E4}`→`.` `{80EC}`→`,` `{80FC}`→`:` `{80EB}`→`!` `{80F4}`→`'` `{80F6}`→`"` `{80EA}`→`?` `{80FE}`→`-`

## เครื่องมือ

```bash
python tools/dump_corpus3.py   # สแกน ROM → data/FFTA_corpus_raw.json (ใช้เวลานาน)
python tools/categorize3.py    # จัดหมวด → data/categorized_text/ + CATEGORIES_INDEX.md
python tools/gapreport.py      # ออกรายงานข้อความที่ถอดไม่ได้
```

```python
from tools.lzss01 import lzss_decompress, lzss_decompress_ex
plain       = lzss_decompress(rom, off)       # ข้อมูลที่ถอดแล้ว
plain, used = lzss_decompress_ex(rom, off)    # + จำนวนไบต์ input ที่ใช้ไป
```
> ใช้ `lzss_decompress_ex` เสมอเวลาเดินสแกนต่อ ต้องรู้ว่าบล็อกจบตรงไหน ไม่งั้นสแกนกินทั้ง ROM แล้วหยุด (บั๊กนี้เคยเกิดมาแล้ว)

**รูปแบบไฟล์ข้อความ:** `[offset]<TAB>ข้อความ` — `xN` ต่อท้าย offset = ข้อความนั้นซ้ำ N ครั้งในเกม

## การแบ่งงาน (ตอนนี้มี 2 คน)

| สาย | ขอบเขตไฟล์ | ทำอะไร |
|---|---|---|
| **Track A — แปล** | `translation/` (สร้างใหม่) | แปล 3,017 บรรทัดที่พร้อมแล้ว |
| **Track B — ปลดล็อก** | `tools/`, `data/`, `FFTA_Recon_Analysis.md` | หาฟอนต์ + เก็บ 394 บรรทัดที่ขาด |

**อย่าข้ามสาย** ถ้าจำเป็นต้องแก้ไฟล์ของอีกสายให้บอกกันก่อน

ทั้งสองสายทำขนานกันได้ ไม่บล็อกกัน — Track A ไม่ต้องรอ Track B เสร็จ

## สิ่งที่ยังไม่รู้ (อย่าเผลอเคลมว่ารู้)

1. **ฟอนต์อยู่ตรงไหน** — ไล่ `LZ77UnCompVram` ครบ 5 จุดแล้วไม่เจอ เหลือ `LZ77UnCompWram` (58 จุด, `0x141890`) และ `CpuSet` (753 จุด, `0x14186C`) ที่ยังไม่ไล่
2. **394 บรรทัดที่ถอดไม่ได้** — มี back-reference อีกแบบที่ไม่มี marker `0x32` เบาะแสอยู่ใน README หัวข้อ "ปัญหา 1"
3. **ยังไม่แยกว่าข้อความไหนฝังในกราฟิก** (โลโก้ ป้ายเมนูบางอัน) พวกนั้นต้องแก้เป็นรูป ไม่ใช่แก้ข้อความ
4. **byte budget ตอนแปะข้อความกลับ** ยังไม่ได้สำรวจ — ภาษาไทยยาวกว่าอังกฤษ อาจต้องหาที่ว่างเพิ่ม

## Skill ที่เกี่ยวข้อง

- `skills/game-text-recon/SKILL.md` — อยู่ใน repo แล้ว (ระเบียบวิธีแกะข้อความเกม)
- `complex-script-game-font` — เฟสฝังฟอนต์ไทย (จัดการสระ/วรรณยุกต์ซ้อน) อยู่ใน Claude Code
- `game-text-localize` — เฟสแปลและแปะกลับ อยู่ใน Claude Code
