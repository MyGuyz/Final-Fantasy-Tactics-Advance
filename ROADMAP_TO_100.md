# จะให้ครบ 100% จริง ๆ ต้องทำยังไง

สถานะปัจจุบัน: ถอดสมบูรณ์ 5,284 / 5,670 บรรทัด (93.2%) — เหลือ **386 บรรทัด** ที่ยังถอดไม่ออก (รายการอยู่ใน `categorized_text/_INCOMPLETE_needs_work.txt`) — รายละเอียดล่าสุด (รวม ground-truth validation จาก save state จริง) อยู่ใน README.md หัวข้อ "ปัญหา 1"

ปัญหาเดียวที่เหลือคือ: **มีรูปแบบเข้ารหัส/บีบอัดอีกแบบที่ยังถอดไม่ได้** (ไม่มี marker `0x32` ให้จับ)

มี 3 ทางเลือก เรียงจากที่ผมแนะนำที่สุด

---

## ทางที่ 1 — ใช้ emulator ดึงข้อความจาก RAM (แนะนำที่สุด, ชัวร์ 100%)

**ทำไมถึงชัวร์:** เกมต้อง**ถอดรหัสข้อความเองอยู่แล้ว**ก่อนวาดลงจอ ข้อความที่ถอดเสร็จจะโผล่ใน RAM เสมอ เราแค่ไปอ่านตรงนั้น — ไม่ต้องเดารูปแบบบีบอัดเลย ผ่านหรือไม่ผ่านรู้ทันที

**ขั้นตอน:**

1. ติดตั้ง **mGBA** (ฟรี, open source) — https://mgba.io
2. เปิดเกม เล่นไปจนถึงฉากที่มีข้อความที่ยังถอดไม่ได้ (ดู offset จาก `_INCOMPLETE_needs_work.txt` เช่นฉากร้านค้าที่ขึ้นว่า "You can't...")
3. เปิด **Tools → Game State Views → View Memory** ดูย่าน EWRAM (`0x02000000`–`0x0203FFFF`) ตอนกล่องข้อความแสดงอยู่
4. หาไบต์ที่เป็นข้อความถอดแล้ว (จะเห็นเป็น pattern `80 Cx / 80 Dx` เรียงกันแบบที่เรารู้จัก)
5. จดที่อยู่ RAM นั้นไว้ แล้วใช้ **mGBA scripting (Lua)** ดัมพ์อัตโนมัติทุกครั้งที่บัฟเฟอร์เปลี่ยน

สคริปต์ Lua คร่าว ๆ ที่ใช้ได้:

```lua
-- mGBA: Tools > Scripting  แล้ววางโค้ดนี้
local ADDR = 0x02000000   -- แก้เป็นที่อยู่บัฟเฟอร์ข้อความที่หาเจอในขั้นตอน 4
local LEN  = 0x400
local seen = {}
local f = io.open("ffta_ram_dump.txt", "a")

function tick()
  local s = ""
  for i = 0, LEN-1 do
    s = s .. string.format("%02X ", emu:read8(ADDR + i))
  end
  if not seen[s] then
    seen[s] = true
    f:write(s .. "\n")
    f:flush()
  end
end

callbacks:add("frame", tick)
```

จากนั้นส่งไฟล์ `ffta_ram_dump.txt` กลับมา ผมถอดเป็นข้อความให้ได้ทันที (charmap ยืนยันแล้ว 100%)

**ข้อดี:** ได้ข้อความจริงแบบที่เกมแสดงจริง ไม่มีทางผิด
**ข้อเสีย:** ต้องเล่นเกมให้ถึงฉากนั้น ๆ (แต่ใช้ save state ข้ามได้)

---

## ทางที่ 2 — แกะรูปแบบบีบอัดต่อแบบ static (ผมทำต่อได้ แต่ไม่การันตี)

**เบาะแสที่หาได้แล้วในรอบนี้** (มีประโยชน์จริง ไม่ใช่เดาลอย ๆ):

เอาสองสตริงที่ขึ้นต้นเหมือนกันมาเทียบไบต์ต่อไบต์:

| | A (`0x49b767`) | B (`0x49b407`) |
|---|---|---|
| ค่าที่พบ | `A00B 9015 801F 8027 802B 802D` | `A00D 9017 8021 8029 802B 8031 803B 803F` |
| 12 บิตล่าง | 11, 21, 31, 39, 43, 45 | 13, 23, 33, 41, 43, 49, 59, 63 |
| 4 บิตบน | A, 9, 8, 8, 8, 8 | A, 9, 8, 8, 8, 8, 8, 8 |

**สิ่งที่พิสูจน์ได้:** B มีกลุ่มพิเศษแทรกเพิ่ม 1 ชุดตอนต้น และค่าทุกตัวหลังจากนั้น **เพิ่มขึ้นพอดี +2 เท่ากันหมด** (11→13, 21→23, 31→33, 39→41)

แปลว่าตัวเลข 12 บิตล่างคือ **offset สะสมที่เลื่อนตามความยาวข้อมูลก่อนหน้า** ซึ่งเป็นลายเซ็นของ **back-reference แบบ LZ** (ชี้กลับไปยังข้อมูลที่ถอดไปแล้ว) ส่วน 4 บิตบน (A/9/8) น่าจะเป็น length หรือ type

**สิ่งที่ยังไม่รู้:** หน่วยของ offset (นับเป็นไบต์ของ input? ของ output? นับเป็นตัวอักษร?) และความหมายที่แน่นอนของ 4 บิตบน — ต้องลองเดา-ทดสอบวนไปจนกว่าผลถอดจะเป็นภาษาอังกฤษที่อ่านรู้เรื่องทั้ง 394 บรรทัด

**ข้อดี:** ไม่ต้องพึ่ง emulator, ได้ตัวถอดที่ใช้ซ้ำได้ตอนแปะข้อความกลับ
**ข้อเสีย:** อาจใช้เวลานานและไม่การันตีว่าจะแตก

---

## ทางที่ 3 — ไล่โค้ด ARM ของฟังก์ชันถอดข้อความ (ชัวร์แต่ช้าที่สุด)

หาฟังก์ชันที่อ่านสตริงแล้ววาดตัวอักษร แล้วอ่าน assembly ว่ามันตีความไบต์พวกนี้ยังไง — ได้คำตอบที่ถูกต้องแน่นอนเพราะเป็นตรรกะของเกมเอง

**จุดเริ่มที่มีอยู่แล้ว:** ตาราง BIOS wrapper ที่ `0x141864–0x14188C` และ dispatcher ที่ `0x0801A15C` (ทั้งคู่หาเจอแล้วในรอบก่อน) — ต้องหา cross-reference จากตรงนี้ไปยัง text renderer

**ข้อเสีย:** ต้องอ่าน assembly THUMB จำนวนมาก ใช้เวลาสูงสุดในสามทาง

---

## สรุปคำแนะนำ

| ทาง | ความชัวร์ | เวลา | ใครทำ |
|---|---|---|---|
| **1. emulator ดึง RAM** | **สูงสุด** | ปานกลาง | คุณเล่น + ผมถอดให้ |
| 2. แกะ static ต่อ | ปานกลาง | ปานกลาง–นาน | ผมทำได้เลย |
| 3. ไล่ ARM | สูง | นานสุด | ผมทำได้เลย |

**แนะนำ:** เริ่มทางที่ 1 ก่อน เพราะเป็นทางเดียวที่**การันตีผลลัพธ์** ระหว่างที่คุณเซ็ตอัพ emulator ผมลุยทางที่ 2 คู่ขนานไปได้ — ถ้าแตกก่อนก็จบเร็ว ถ้าไม่แตกก็ยังมีทางที่ 1 รับประกันอยู่

---

## หมายเหตุสำคัญ

**88.4% ที่ได้แล้วเริ่มแปลได้เลย** ไม่ต้องรอ 394 บรรทัดที่เหลือ เพราะ:
- ทั้ง 394 บรรทัดมี offset กำกับครบ กลับมาเติมทีหลังได้
- ส่วนที่ค้างส่วนใหญ่เป็นข้อความร้านค้า/ระบบ ไม่ใช่เนื้อเรื่องหลัก (เนื้อเรื่องหลักกู้คืนมาแล้วใน `story_dialogue.txt`)

**อีกเรื่องที่ยังค้างและควรทำก่อนแปะข้อความกลับ:** ยังหา**ตำแหน่งฟอนต์**ไม่เจอ (ดู `FFTA_Recon_Analysis.md` หัวข้อ 5) — ซึ่งทางที่ 1 แก้ได้พร้อมกัน เพราะเปิด Tile Viewer ใน mGBA จะเห็นฟอนต์ทันที
