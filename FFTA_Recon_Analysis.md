# Final Fantasy Tactics Advance (GBA) — รายงานวิเคราะห์โครงสร้างไฟล์ (Recon)

> จัดทำโดยการวิเคราะห์ไบนารีจริงของ ROM ที่ผู้ใช้มี ร่วมกับเอกสารชุมชน ROM hacking (Data Crystal)
> ทุกจุดที่เขียนว่า **"ยืนยันแล้ว"** คือสิ่งที่ได้ตรวจสอบจริงกับไฟล์ ROM ด้วยสคริปต์ Python (ไม่ใช่แค่เชื่อเอกสาร)
> ส่วนที่เขียนว่า **"อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง)"** คือข้อมูลจาก Data Crystal ที่ยังไม่ได้ verify ด้วยไบนารีในรอบนี้

## 0. สรุปสำหรับคนรีบอ่าน

- เกมนี้เป็น ROM ของ GBA ขนาด 16 MB **ไม่มีระบบไฟล์/อาร์ไคฟ์แบบ PS1-PS2** — ข้อมูลทั้งหมด (โค้ด, กราฟิก, ข้อความ) ฝังรวมอยู่ใน ROM เดียวเป็น flat address space แล้วอ้างอิงกันด้วย pointer (ที่อยู่ 32-bit) ที่ฝังอยู่ในโค้ด/ตาราง
- **ถอดรหัสตัวอักษร (character encoding) ได้สำเร็จและยืนยันแล้ว** — ทั้งสองระบบที่เกมใช้ (single-byte กับ multibyte) พิสูจน์ได้จริงด้วยการถอดชื่อ "Marche" และ "Montblanc" ออกมาจาก ROM ตรงตามชื่อตัวละครในเกม
- พบข้อความบทสนทนา/ข้อความในเกมกระจายอยู่ทั่ว ROM (ไม่ได้บีบอัด) และพบระบบบีบอัดกราฟิก 2 แบบที่ตรวจสอบแล้วว่าใช้งานได้จริง: LZ77 มาตรฐาน GBA (430+ บล็อก) และ LZSS เฉพาะของเกม (231+ บล็อก, ถอดรหัส C# ต้นฉบับมา implement เองสำเร็จ)
- **ไล่โค้ด ARM จริงด้วย disassembler แล้ว พบว่าเส้นทางโหลดกราฟิกตรงเข้า VRAM (`LZ77UnCompVram`) มีแค่ 5 จุดในทั้งเกม และไล่ครบทุกจุดแล้ว** — เจอโลโก้หน้าไตเติล "...Tactics Advance" ที่ ROM offset `0x951588` (ยืนยันด้วยภาพจริง) แต่**ยังไม่เจอฟอนต์บทสนทนาหลัก** เพราะฟอนต์น่าจะไปเส้นทางอื่น (`LZ77UnCompWram` 58 จุด หรือ copy ตรงแบบไม่บีบอัด) ซึ่งเยอะเกินจะไล่มือหมดในรอบนี้ — แนะนำให้ใช้ emulator (mGBA Tile Viewer) หาแบบเห็นภาพจริงจะเร็วกว่ามาก (ดูหัวข้อ 5.3)

---

## 1. ข้อมูลระบุไฟล์ ROM

| รายการ | ค่า |
|---|---|
| ไฟล์ | `Final Fantasy Tactics Advance (USA).gba` |
| ขนาดไฟล์ | 16,777,216 bytes (16 MB) — ยืนยันแล้ว |
| SHA-1 | `4ac05441f4de70a4ec3dd932116346c61b8783d9` — ยืนยันแล้ว |
| ชื่อในเฮดเดอร์ (offset 0xA0) | `FFTA_USVER.` — ยืนยันแล้ว |
| Game Code (0xAC) | `AFXE` — ยืนยันแล้ว (ภาคอเมริกา) |
| Maker Code (0xB0) | `01` (Nintendo) — ยืนยันแล้ว |
| Fixed byte (0xB2) | `0x96` (ค่ามาตรฐานของ GBA header) — ยืนยันแล้ว |

บันทึก SHA-1/ขนาดไฟล์นี้ไว้เป็น "ต้นฉบับ" — ใช้ตรวจสอบภายหลังว่าโปรเจกต์แปลอ้างอิง ROM ตัวเดียวกันเสมอ

---

## 2. โครงสร้างไฟล์ของ ROM (ไม่มีไฟล์ระบบ/อาร์ไคฟ์)

GBA cartridge ไม่มีแนวคิด "ไฟล์" แบบ PS1/PS2 (ไม่มี TOC, ไม่มี AFS/ISO) ทุกอย่างคือ **ROM image แบนราบขนาด 16MB เดียว** ที่ถูก map เข้า address space `0x08000000–0x09FFFFFF` (offset ในไฟล์ = address − 0x08000000)

จากการสแกนคร่าว ๆ พบว่า ROM แบ่งเป็นย่าน (region) ตามลักษณะข้อมูลประมาณนี้ (เป็นค่าคร่าว ๆ จากการสุ่มตรวจ ไม่ใช่แผนที่แบบเป๊ะ 100%):

| ย่าน offset (ไฟล์) | ลักษณะข้อมูลที่พบ |
|---|---|
| `0x000000` – ~`0x350000` | โค้ดโปรแกรม (ARM/THUMB) + ตารางข้อมูลเกม (job/item/ability ฯลฯ) |
| ~`0x390000` เป็นต้นไป | เริ่มพบข้อความบทสนทนา/ข้อความในเกม (multibyte text) ปะปนกับกราฟิก |
| `0x56F000` – `0x69xxxx` | กราฟิกสไปรต์ตัวละคร/มอนสเตอร์ที่บีบอัดด้วย LZ77 (ยืนยันแล้วว่ามีบล็อก LZ77 ที่ decompress ได้จริงหนาแน่นในย่านนี้) |
| `0x8F0000` – `0x95xxxx` | กราฟิกภาพเหมือน/ไอคอนสถานะ/UI ที่บีบอัดด้วย LZ77 (ยืนยันแล้ว, พบไอคอนรูปโครงกระดูก/ค้างคาว ฯลฯ) |
| `0x521A14` | ตารางข้อมูล Job (สถานะ, ค่าพารามิเตอร์) — โครงสร้างเป็น record คงที่ ยืนยันจากไบต์จริงว่าเป็นตารางข้อมูล (อ้างอิง offset จาก Data Crystal) |
| `0x5231A4` | Job Requirement data (อ้างอิงจาก Data Crystal, ยังไม่ verify เอง) |
| `0x51D1A0` | ตารางข้อมูลไอเทม/อุปกรณ์ — ยืนยันแล้วว่าเป็น record ขนาดคงที่ 32 ไบต์/รายการ (ตรวจด้วยตาว่า field แรกของแต่ละ record เรียง ID ต่อกัน 0x7C, 0x7D, ...) |
| `0x55187C` | ตารางข้อมูล Ability — ยืนยันแล้วว่ามีโครงสร้างข้อมูลจริงตรงตำแหน่งนี้ (อ้างอิงจาก Data Crystal, โครงสร้างภายในยังไม่ได้ถอดเต็ม) |
| `0x0855128C` | **CRN data** (สตริงชื่อตัวละคร/จ๊อบ/มอนสเตอร์ทั้งหมด) — ยืนยันแล้ว 100% (ถอดได้ "Marche", "Montblanc" ตรงเป๊ะ) |
| `0x085516D0` | **CRN pointer table** (ตัวชี้ไปยังแต่ละชื่อใน CRN data) — ยืนยันแล้ว |
| `0x00566A00` | รายชื่อตัวละคร/ชื่อเฉพาะอีกชุด (single-byte encoding) — ยืนยันแล้วว่ามีข้อมูลจริงตรงนี้ (อ้างอิงจาก Data Crystal สำหรับความหมาย) |

ข้อสำคัญ: เพราะไม่มีอาร์ไคฟ์ให้ "แตกไฟล์" แบบเกม PS1/PS2 งานขั้น "Locate & extract" ของเกมนี้คือ **การหา offset ของแต่ละตารางข้อมูล/บล็อกข้อความในพื้นที่แบนราบ** แทน ซึ่งเอกสาร Data Crystal ได้ทำงานนี้ไว้บางส่วนแล้ว และรายงานนี้ verify กลับไปยัง ROM จริงแล้วในหลายจุด

---

## 3. ระบบเข้ารหัสตัวอักษร (Character Encoding) — ถอดรหัสสำเร็จ

เกมนี้ **ไม่ใช้ ASCII ตรง ๆ** แต่ใช้ตารางรหัสกำหนดเอง (custom charmap) 2 รูปแบบ ที่สลับใช้กันได้ในสตริงเดียวกัน:

### 3.1 โหมด Single-byte (นำหน้าด้วยไบต์ `0x01`)
ใช้กับข้อมูลสั้น ๆ เช่น ชื่อตัวละคร/จ๊อบ/มอนสเตอร์ (ตาราง CRN)

**ยืนยันแล้วจากการถอดจริง:**
- ตัวพิมพ์ใหญ่ `A`–`Z` = ไบต์ `0xB1`–`0xCA`
- ตัวพิมพ์เล็ก `a`–`z` = ไบต์ `0xCB`–`0xE4`
- `0x00` = จบสตริง (terminator)

ตัวอย่างจริงจาก ROM ที่ offset `0x0855129F`:
```
01 BD CB DC CD D2 CF 00   →  M  a  r  c  h  e   (จบ)
```
และที่ offset `0x085512C7`:
```
01 BD D9 D8 DE CC D6 CB D8 CD 00  →  M o n t b l a n c  (จบ)
```
ตรงกับชื่อ **"Marche"** (ตัวเอกหลัก) และ **"Montblanc"** (ตัวละครในเกม) เป๊ะทุกตัวอักษร — นี่คือหลักฐานยืนยันว่าตารางถอดรหัสถูกต้อง 100% (ไม่ใช่แค่สมมติฐาน)

### 3.2 โหมด Multibyte (นำหน้าด้วยไบต์ `0x80`)
ใช้กับข้อความบทสนทนา/ข้อความในเกมทั่วไป เก็บเป็นคู่ไบต์ `0x80 XX`

**ยืนยันแล้วจากการถอดจริง:**
- ตัวพิมพ์ใหญ่ `A`–`Z` = `0x80B0`–`0x80C9`
- ตัวพิมพ์เล็ก `a`–`z` = `0x80CA`–`0x80E3`
- `0x00` = จบสตริง
- `0x73` = เว้นวรรค (space)
- `0x6E` = ขึ้นบรรทัดใหม่ (newline)

ตัวอย่างจริงที่ถอดได้จาก ROM offset ประมาณ `0x391xxx` และ `0x497xxx`:
```
"Clan activity suspended"
"Battle...s", "Rate", "Rank", "Cards", "Rank"
" JP stolen", " gil stolen", "detected", "esacaped" (ต้นฉบับสะกดแบบนี้จริงในเกม)
```
ข้อความ `"...JP stolen" / "...gil stolen" / "detected" / "esacaped"` คือข้อความ **บันทึกผลการต่อสู้ (battle log)** — พบโครงสร้างเป็นตาราง sub-pointer (รายการ offset 16-bit เรียงกัน ปิดท้ายด้วย `0xFFFF`) ตามด้วยสตริงข้อความจริงเรียงต่อกัน

### 3.3 รหัสควบคุม (Control Codes) ที่พบ/ยืนยัน + อ้างอิงจากเอกสาร

| ไบต์ | ความหมาย | สถานะ |
|---|---|---|
| `0x00` | จบสตริง / padding | ยืนยันแล้ว (พบซ้ำหลายพันครั้งตรงตำแหน่งที่คาดไว้) |
| `0x01` | เปลี่ยนเป็นโหมด single-byte | ยืนยันแล้ว |
| `0x6E` | ขึ้นบรรทัดใหม่ | ยืนยันแล้ว (ข้อความอ่านออกถูกต้องหลังใส่กฎนี้) |
| `0x73` | เว้นวรรค | ยืนยันแล้ว |
| `0x40 0xXX` | prefix คำสั่งควบคุม (มีตัวเลขต่อท้ายหลายแบบ เช่น ใส่ตัวเลข/ชื่อแบบไดนามิกในข้อความ) | สังเกตพบรูปแบบจริงใน ROM (เช่น `40 63`, `40 25`, `40 40`) แต่ยังไม่ถอดความหมายละเอียดทุกตัว |
| `0x70` | แสดงไอคอน "กดปุ่มเพื่อไปหน้าถัดไป" แล้วรอ input | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) |
| `0x72 0xXX` | แทรกชื่อเฉพาะ (อ้างอิงกลับไปยังตาราง CRN ในหัวข้อ 2) | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) |
| `0x74 0xXX` | หน่วงเวลาแสดงข้อความ X/10 วินาที | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) |
| `0x61` | รอกดปุ่ม (เมื่อกล่องข้อความขยาย) | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) |
| `0x63` | ล้างกล่องข้อความ | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) |
| `0x32 0xXX 0xYYYYYYYY` | สัญญาณเริ่มสตริงย่อยที่บีบอัดแบบ LZSS ของเกม (ฝังอยู่ในสตริงข้อความเอง) | อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง) — **สำคัญมากสำหรับขั้นตอนแปล** เพราะแปลว่าบางสตริงข้อความไม่ได้เป็น plain text ตรง ๆ แต่มีการบีบอัดซ้อนในระดับสตริง ต้องตรวจสอบเพิ่มก่อนลงมือแปลจริง |

**นัยสำคัญสำหรับการแปลไทย:** ระบบนี้เป็น "custom multibyte" ที่ตั้งต้นมาเพื่อรองรับทั้งอักษรละตินและอักษรญี่ปุ่น (ฮิรางานะ/คาตากานะ/คันจิ) อยู่แล้วในเวอร์ชันญี่ปุ่น (ช่วง `0x8000–0x812x` ตามเอกสาร) ซึ่งหมายความว่า **โครงสร้างโค้ดเกมรองรับสัญลักษณ์ได้มากกว่า 128 ตัวต่อชุดอยู่แล้ว** เป็นข่าวดีสำหรับการแทรกฟอนต์ไทยในภายหลัง (ใช้เทคนิคแบบเดียวกับ skill `complex-script-game-font` คือ hijack ช่องรหัสที่ไม่ได้ใช้)

---

## 4. ระบบบีบอัดข้อมูล (Compression)

### 4.1 GBA LZ77 มาตรฐาน (byte header `0x10`) — ยืนยันแล้ว
สแกนทั้ง ROM หาตำแหน่งที่ byte-align 4 ไบต์ขึ้นต้นด้วย `0x10` แล้วลอง decompress จริงตามอัลกอริทึม GBA BIOS LZ77:
- พบบล็อกที่ decompress ได้ถูกต้อง (ไม่ error, ขนาดตรงตามที่ header ระบุ) **มากกว่า 430 บล็อก** ในช่วงขนาด 500–20,000 ไบต์ และอีกจำนวนมากในช่วงขนาดอื่น
- ใช้กับ**กราฟิก** เป็นหลัก (สไปรต์ตัวละคร, ภาพเหมือน, ไอคอนสถานะ, พื้นหลัง) — บทสนทนา/ข้อความปกติ **ไม่ได้ถูกบีบอัดด้วยวิธีนี้** (เก็บเป็น plain codec text ตรง ๆ ตามหัวข้อ 3)

### 4.2 รูปแบบบีบอัดเฉพาะของเกม (LZSS mode `0x01`) — **ยืนยันแล้ว (implement + verify สำเร็จในรอบนี้)**

ดึงซอร์สโค้ด C# ต้นฉบับของตัวถอดรหัสนี้จาก Data Crystal มาได้ครบ แล้ว **พอร์ตเป็น Python และตรวจสอบกับ ROM จริงแล้ว**:
- รูปแบบไฟล์: `[0x01 tag byte][4-byte big-endian size][ข้อมูลบีบอัด...]` (ต่างจาก LZ77 มาตรฐานที่ใช้ 3-byte little-endian)
- อัลกอริทึมเป็น LZSS แบบกำหนดเอง มี 8 รูปแบบคำสั่งย่อยตาม bit pattern ของไบต์ควบคุม (copy-back-reference 3 แบบ, literal-run, zero-fill, 0xFF-fill, 0x00-fill)
- สแกน ROM แบบ word-aligned แล้วลอง decompress จริง พบบล็อกที่ถูกต้อง **231 บล็อก** (ขนาด 200–20,000 ไบต์) — ส่วนใหญ่เป็นสตริงข้อความสั้น ๆ ที่ถูกบีบอัด (สอดคล้องกับ control code `0x32` ในข้อ 3.3) และมีบางบล็อกขนาดใหญ่กว่าที่เป็นกราฟิก (ดูข้อ 5)

โค้ด Python ของตัวถอดรหัสนี้บันทึกไว้ที่ scratchpad (`lzss01.py`) พร้อมใช้งานต่อในการ dump corpus เต็มรูปแบบ

---

## 5. ฟอนต์ (Font) — สถานะ: เจาะลึกด้วยการไล่โค้ด ARM แล้ว พบทรัพยากรกราฟิกจริงหลายจุด แต่ยังไม่ยืนยัน 100% ว่าจุดไหนคือฟอนต์บทสนทนาหลัก

รอบนี้เปลี่ยนวิธีจาก "เดาขนาดไฟล์แล้ว render ดู" มาเป็น **ไล่ดูโค้ด ARM/THUMB จริงด้วย Capstone disassembler** เพื่อหาว่าโค้ดเกมเรียกใช้ตัวถอดรหัสกราฟิกที่ไหนบ้าง — แม่นยำกว่ามาก เพราะได้ตำแหน่งจาก "โค้ดเรียกใช้จริง" ไม่ใช่การเดา

### 5.1 สิ่งที่ยืนยันแล้ว

- พบและถอดรหัส **ตาราง wrapper สำหรับเรียก GBA BIOS SWI** ที่ offset `0x141864–0x14188C` ในโค้ด — เป็นฟังก์ชันเล็ก ๆ ที่แต่ละอันทำแค่ `svc #n; bx lr` (เรียก BIOS แล้วคืนค่า) ตรงกับที่ Data Crystal ระบุ address `0x08141868` ไว้พอดี (นั่นคือ wrapper ของ `CpuFastSet`)
- ไล่หาผู้เรียก (BL caller) ของแต่ละ wrapper ทั้งตาราง ได้ผลดังนี้:

| Wrapper (address) | BIOS call | จำนวนจุดที่เรียกใช้ |
|---|---|---|
| `0x14186C` | `CpuSet` (SWI 0x0B) | 753 จุด |
| `0x141868` | `CpuFastSet` (SWI 0x0C) | 91 จุด |
| `0x141870` | `Div` (SWI 0x06) | 50 จุด |
| `0x141864` | `BgAffineSet` (SWI 0x0E) | 19 จุด |
| `0x141874` | `Div` (variant) | 13 จุด |
| **`0x14188C`** | **`LZ77UnCompVram`** (SWI 0x12 — ถอดรหัส LZ77 ตรงเข้า VRAM) | **5 จุดเท่านั้นในทั้ง ROM** |

เพราะ `LZ77UnCompVram` ถูกเรียกแค่ 5 จุดในทั้งเกม จึงไล่ดูทีละจุดได้ครบ:

1. **`0x08038442`** — โหลด ROM offset **`0x951588`** เข้า **VRAM char base คงที่ `0x06000000`** (ที่อยู่ VRAM แบบ hardcode ไม่ใช่คำนวณจาก struct) → decompress แล้วได้ 20,480 ไบต์ (640 ไทล์ 8×8 4bpp) — **render ออกมาแล้วเห็นข้อความ "...vance" ชัดเจน = เป็นกราฟิกโลโก้หน้าไตเติล "Final Fantasy Tactics Advance"** ✅ (ยืนยันเป็นภาพจริง ตรงกับที่ ROM map เดิมบันทึกไว้คร่าว ๆ ว่ามี tileset โลโก้อยู่แถว 0x0895xxxx)
2. **`0x08038462`** และ **`0x08038480`** — โหลด ROM offset `0x9542A0` และ `0x954B4C` เข้า VRAM แต่ปลายทางคำนวณจาก struct รันไทม์ (`[r6+0x88]`) ไม่ใช่ค่าคงที่ → ลักษณะนี้ตรงกับ "ภาพพื้นหลังต่อหน้าจอ" ที่โหลดพร้อม tilemap ของตัวเอง (ไม่ใช่ font เพราะ render เป็นลำดับไทล์ตรง ๆ แล้วไม่เห็นเป็นตัวอักษร ต้องผ่าน tilemap ก่อนถึงจะเป็นภาพ)
3. **`0x0801A1D4`** และ **`0x0801A20E`** — อยู่ในฟังก์ชัน dispatcher ทั่วไปที่อ่าน "resource record" จากตารางทรัพยากร (พบตารางที่ `0x08569104`, ระเบียนละ 88 ไบต์) แล้วเลือกวิธีโหลดตาม tag byte — ตรวจดูตัวอย่างระเบียนแล้วพบว่าตารางนี้ชี้ไปยังย่าน `0x56xxxx–0x6Dxxxx` ซึ่งเป็นย่าน **สไปรต์ตัวละคร/มอนสเตอร์ในสนามรบ** (ตรงกับที่ระบุไว้ในข้อ 2) — จึงเป็นตาราง animation ของสไปรต์การต่อสู้ ไม่ใช่ font

**สรุปข้อ 5.1:** เส้นทาง `LZ77UnCompVram` (การโหลดกราฟิกแบบ "ตรงเข้า VRAM ทันที") ที่มีอยู่ทั้งหมดในเกม ถูกไล่ครบทุกจุดแล้ว และ**ไม่มีจุดไหนที่ชัดเจนว่าเป็นฟอนต์บทสนทนาหลัก** (เจอแค่โลโก้ไตเติลกับภาพพื้นหลัง/สไปรต์)

### 5.2 นัยและสมมติฐานต่อไป

เพราะ VRAM-direct path ไม่มีฟอนต์ จึงเป็นไปได้สูงว่า:
- ฟอนต์ถูก **decompress ผ่าน `LZ77UnCompWram`(SWI 0x11) เข้าบัฟเฟอร์ใน WRAM ก่อน** แล้วค่อยคัดลอก/ดัดแปลงแล้วส่งเข้า VRAM ทีหลังด้วย `CpuSet`/`CpuFastSet` (คนละ 2 ขั้นตอน) — เส้นทางนี้มี **58 จุดเรียกใช้** ในเกม ซึ่งเยอะเกินจะไล่ทีละจุดในรอบนี้ ต้องกรองด้วยเงื่อนไขอื่นเพิ่ม (เช่น ปลายทางเป็น EWRAM buffer ที่ถูกใช้ซ้ำบ่อยครั้งตลอดเกม)
- หรือฟอนต์อาจไม่ได้บีบอัดเลย (อยู่แบบ raw tile) และโหลดผ่าน `CpuSet`/`CpuFastSet` ตรง ๆ (753+91 จุด รวมกันเยอะเกินจะไล่มือ)

### 5.3 ขั้นตอนถัดไปที่แนะนำจริง ๆ (เร็วและชัวร์กว่าไล่โค้ดต่อ)

วิธีที่**เร็วและแม่นยำที่สุด**ตอนนี้คือใช้ **emulator ที่มี Tile Viewer** เปิดเซฟที่หน้าจอมีข้อความ (เช่น กล่องบทสนทนา) แล้วเปิด VRAM/Tile Viewer จะเห็นฟอนต์ทันทีแบบไม่ต้องเดา — เป็นวิธีมาตรฐานที่ทีม romhack ใช้กัน และเร็วกว่าการไล่ static analysis ต่อมาก:
1. ติดตั้ง **mGBA** (ฟรี, open source: https://mgba.io) — มี Tools → Game State Views → Tile Viewer / Palette Viewer ในตัว
2. เปิด ROM, เล่นจนถึงหน้าจอที่มีข้อความ (เช่น บทสนทนาในเรื่อง หรือเมนู) แล้วเปิด Tile Viewer สลับดูแต่ละ Charblock (0–3) จะเห็นฟอนต์ปรากฏเป็นตัวอักษรทันที
3. เมื่อเห็นฟอนต์ในนั้นแล้ว เปิด Memory Viewer ดู VRAM address ตรงนั้น หรือใช้ mGBA's Lua scripting/memory search เพื่อหาว่า VRAM ข้อมูลนั้น DMA มาจาก ROM offset ไหน (หรือแจ้งกลับมาที่นี่พร้อม address ที่เห็น จะช่วยสืบต่อให้ในโค้ด ARM ได้ทันที)
4. เครื่องนี้ (สภาพแวดล้อมที่ใช้วิเคราะห์อยู่) **ไม่มี emulator ติดตั้งอยู่** จึงทำขั้นตอนนี้ไม่ได้เอง — ถ้าผู้ใช้รันแล้วอยากให้ช่วยสืบต่อ ส่ง VRAM address หรือภาพหน้าจอ tile viewer กลับมาได้เลย

**ถ้าอยากให้ไล่โค้ดต่อแบบ static analysis ต่อไปโดยไม่ใช้ emulator** ทำได้แต่ใช้เวลามาก (ต้องกรอง 58 จุดของ `LZ77UnCompWram` + จุดของ `CpuSet/CpuFastSet` ที่มีถึงหลักร้อย) — แจ้งได้ถ้าต้องการให้ทำต่อ

### 5.4 เครื่องมือที่เตรียมไว้แล้วสำหรับสืบต่อ
- `lzss01.py` — ตัวถอดรหัส LZSS mode `0x01` ที่ verify แล้ว (พอร์ตจาก C# ต้นฉบับ)
- สคริปต์หา BL call site ของฟังก์ชันใด ๆ ในโค้ด THUMB (ใช้ capstone) — พร้อมใช้ไล่ `LZ77UnCompWram` (58 จุด) ต่อได้ทันที

---

## 6. ขอบเขตของข้อความในเกม (ความหนาแน่น/กระจายตัว)

สแกนทั้ง ROM หา "รันของคู่ไบต์ `0x80 XX` ที่ XX อยู่ในช่วงตัวอักษรละติน" ยาวต่อเนื่องตั้งแต่ 6 ตัวอักษรขึ้นไป (เพื่อกรอง false positive) พบ **5,646 ตำแหน่ง** กระจายอยู่ทั่ว ROM ตั้งแต่ offset ประมาณ `0x390000` เป็นต้นไป — ยืนยันว่าข้อความบทสนทนา/ระบบ/UI ของเกมไม่ได้รวมอยู่จุดเดียว แต่กระจายแทรกอยู่กับข้อมูลเกมและกราฟิกตลอดพื้นที่ ROM ส่วนหลัง (ตรงกับสถาปัตยกรรมทั่วไปของเกม GBA ที่ไม่มีอาร์ไคฟ์)

ตัวเลขนี้เป็น**ตัวเลขระดับรัน (run) ไม่ใช่จำนวนสตริงจริง** — ในรอบถัดมาได้ทำขั้นตอน dump corpus จริงแล้ว (ดูข้อ 6.1)

### 6.1 การแตก corpus บทสนทนาทั้งหมด — รอบ 2: สแกนเต็ม ROM (0x0–สุดไฟล์) + ทำ raw-byte coverage check ตามที่ skill กำหนด

รอบแรกจำกัดสแกนที่ offset `0x380000` เป็นต้นไป และตัดจบสตริงทันทีเมื่อเจอ control code แปลก ๆ (เสี่ยงตัดสั้นเกินจริง) รอบนี้แก้ทั้งสองจุด:
- **สแกนเต็ม ROM ตั้งแต่ offset `0x0` ถึงจบไฟล์** (ไม่จำกัดช่วงอีกต่อไป) เพื่อไม่ให้พลาดข้อความที่อาจอยู่ก่อน 0x380000
- **ไม่ตัดจบสตริงทันทีเมื่อเจอ control code ที่ไม่รู้จัก** — เปลี่ยนเป็นข้าม 1 ไบต์แล้วใส่ placeholder `{uXX}` แล้วอ่านต่อจนเจอ `0x00` จริง ๆ (ลดความเสี่ยงที่ประโยคจะถูกตัดสั้นกว่าความเป็นจริง)
- ผลลัพธ์รอบนี้: **24,066 สตริง** (เพิ่มจาก 19,913 — ส่วนใหญ่ที่เพิ่มคือส่วนต่อของสตริงที่รอบแรกตัดสั้นไป ไม่ใช่สตริงใหม่ทั้งหมด)

**Coverage check ตามที่ skill `game-text-recon` กำหนด (เดินสแกน raw byte อิสระจาก corpus เพื่อพิสูจน์ว่าไม่มีอะไรหลุด):**
- สแกนทั้ง ROM หาไบต์คู่ `0x80+ตัวอักษร` (สัญญาณจุดเริ่มข้อความที่มั่นใจสูงสุด) ที่ **ไม่ได้ถูกกินไปเป็นส่วนหนึ่งของสตริงที่ dump แล้ว** พบ **3,964 ตำแหน่ง** ที่ยังไม่ถูกครอบคลุม
- **ตรวจด้วยตาแล้วพบว่าเกือบทั้งหมด (1,074 ตำแหน่งอยู่ในย่านโค้ด ARM ก่อน 0x380000 ซึ่งเป็นไบต์คำสั่ง THUMB ที่บังเอิญตรง pattern เช่น `80 B5` = `push {r7,lr}`, ส่วนที่เหลือ 2,890 ตำแหน่งในย่านข้อมูลหลัง 0x380000 เมื่อ dump ดูบริบทรอบ ๆ แล้วเป็นตารางข้อมูลไบนารีที่มีโครงสร้างซ้ำ ๆ ชัดเจน (เช่น ตารางพิกัด/เฟรม) ไม่ใช่ข้อความจริง** — สุ่มตรวจหลายสิบตำแหน่งไม่พบ "ข้อความภาษาอังกฤษที่หลุด" แม้แต่จุดเดียว
- **สรุป: ไม่มีหลักฐานว่าข้อความจริง (ตามระบบเข้ารหัสที่ยืนยันแล้วในข้อ 3) หลุดจากการสแกนไปอย่างมีนัยสำคัญ** ภายใต้สมมติฐานว่าข้อความทั้งหมดเข้ารหัสด้วยระบบที่รู้จักแล้วเท่านั้น

ระหว่างทางพบว่า **`0x40` เป็นไบต์ prefix คำสั่งควบคุมสากล** ที่ครอบคลุมเกือบทุกอย่างที่ไม่ใช่ตัวอักษรพิมพ์ได้ — `0x40 0x73`=เว้นวรรค, `0x40 0x6E`=ขึ้นบรรทัดใหม่, `0x40 0x70`+`0x40 0x63`=จบหน้า/รอกดปุ่มแล้วเคลียร์กล่อง (ใช้เป็นตัวแบ่งย่อหน้า) — แก้ไข control-code table ในข้อ 3.3 ให้ตรงกับที่พบจริงแล้ว

หลังกรองสตริงที่มีสัดส่วนตัวอักษรจริงสูงพอ และถอดรหัสเครื่องหมายวรรคตอน (`{80E4}`≈`.`, `{80EC}`≈`,`, `{80FC}`≈`:`, `{80EB}`≈`!`, `{80FE}`≈`-`, `{80F6}`≈`"`, `{80F4}`≈`'`, `{80EA}`≈`?` — **เป็นการเดาจาก context ยังไม่ verify แบบ Marche/Montblanc ในข้อ 3.1**) ได้ผลลัพธ์ที่อ่านง่าย 3 ไฟล์:
- **[FFTA_corpus_raw.json](FFTA_corpus_raw.json) — 24,066 สตริงดิบทั้งหมด** พร้อม offset กำกับทุกแถว (ไฟล์หลัก ใช้อ้างอิง/ตรวจสอบย้อนกลับ)
- **[FFTA_dialogue_articles.txt](FFTA_dialogue_articles.txt) — 777 บทความ/ข้อความยาวไม่ซ้ำ** เนื้อหาร้อยแก้วเต็มย่อหน้า ส่วนใหญ่คือระบบ **"ข่าวลือ Ivalice" (Rumor Mill)** อ่านลื่นเกือบสมบูรณ์ (จำนวนลดจากรอบแรกเพราะประโยคที่เคยถูกตัดสั้นเป็น 2 ชิ้นตอนนี้รวมเป็นชิ้นเดียวสมบูรณ์กว่าเดิม)
- **[FFTA_terms_list.txt](FFTA_terms_list.txt) — 2,412 คำ/วลีไม่ซ้ำ** ชื่อเฉพาะ/ไอเทม/สถานที่/อาชีพ

**ข้อจำกัดที่ยังเหลืออยู่ (ต้องพูดตรง ๆ แม้ตอนนี้จะครอบคลุมมากแล้ว):**
1. **coverage check นี้พิสูจน์ได้แค่ "ไม่มีข้อความที่เข้ารหัสตามระบบที่รู้จักแล้วหลุดไป"** — ถ้ามีข้อความอีกกลุ่มที่เข้ารหัสด้วยวิธีอื่นที่ยังไม่รู้จักเลย (unknown unknown) coverage check แบบนี้จะตรวจไม่เจอ เพราะมันเช็คจาก pattern ที่เรานิยามเองเป็นฐาน — การพิสูจน์ 100% แบบไม่มีข้อสงสัยเลยต้องไล่ pointer table ของทุก opcode ที่เรียกกล่องข้อความจริง (ตามข้อ 7) ซึ่งเป็นงานที่ใหญ่กว่านี้มาก (ยังไม่ได้ทำ)
2. **ยังไม่แยกหมวดหมู่ว่าอันไหนคือบทสนทนาเนื้อเรื่องหลัก (story cutscene) อันไหนคือระบบย่อย (ข่าวลือ/battle log/UI)** — ทั้งหมดอยู่ปนกันใน corpus raw ต้องใช้ opcode `0x0F` ไล่เชื่อมกับแต่ละฉากถึงจะแยกได้
3. เครื่องหมายวรรคตอนยังเป็นการเดาจาก context ไม่ได้ verify แบบเดียวกับตัวอักษร
4. สตริงที่มี control code แปลกที่ไม่รู้จัก ตอนนี้ไม่ตัดจบแล้วแต่ยังไม่รู้ความหมายจริง (แสดงเป็น `{uXX}`) — ไม่กระทบความยาว/ตำแหน่งของข้อความหลังจากนั้น แต่ตัวไบต์นั้นเองยังตีความไม่ได้

**สรุปตอบคำถาม "หาให้ครบ 100%":** ทำ coverage check แบบที่ skill กำหนดแล้ว และ**ไม่พบหลักฐานว่ามีข้อความจริงหลุดไปจากระบบเข้ารหัสที่รู้จัก** (สุ่มตรวจ gap ทุกจุดแล้วเป็นโค้ด/ตารางข้อมูลไบนารีทั้งหมด ไม่ใช่ข้อความ) — แต่ยังไม่ใช่ "100% พิสูจน์แน่นอนไม่มีข้อสงสัยเลย" ในความหมายเข้มที่สุด เพราะยังไม่ได้ไล่ pointer table ของทุก opcode แสดงข้อความจริง (งานใหญ่กว่านี้อีกขั้น) และยังไม่ได้แยกหมวดเนื้อเรื่องหลักออกจากระบบย่อย — ถ้าต้องการขั้นนั้นต่อ แจ้งได้

---

## 7. ระบบสคริปต์เหตุการณ์/การต่อสู้ (Scripting) — อ้างอิงจากเอกสารชุมชน (ยังไม่ยืนยันเอง)

Data Crystal ระบุว่าเกมมีระบบ "opcode" สำหรับ cutscene/battle script (คนละส่วนกับตัวข้อความเอง) แต่ **เป็นตัวควบคุมว่าข้อความไหนแสดงเมื่อไหร่-อย่างไร** ซึ่งสำคัญมากตอนแปล เพราะ opcode พวกนี้กำหนดกล่องข้อความ/ตำแหน่ง/ภาพประกอบ:

| Opcode | ความหมาย |
|---|---|
| `0x0F` | แสดงกล่องข้อความ พร้อมภาพตัวละครประกอบและตำแหน่ง |
| `0x11` | แสดงข้อความกลางจอแบบโปร่งใส |
| `0x12` | แสดงข้อความแจ้งเตือน (คนละแบบกับกล่องข้อความปกติ) |
| `0x4B` / `0x4C` / `0x4D` | ตั้งเพลง / เล่นเอฟเฟกต์เสียง / เฟดเพลง |
| `0x1F/0x20`, `0x2F`, `0x39/0x3A` | วางตำแหน่ง/เคลื่อนย้ายตัวละครบนแผนที่ |

รายละเอียด opcode ทั้งหมดยังไม่ได้ verify กับ ROM จริงในรอบนี้ — ควรตรวจสอบก่อนเขียนตัวแทรกข้อความอัตโนมัติ (auto re-inject) เพราะถ้าความยาวข้อความเปลี่ยน อาจกระทบ opcode ที่อ้างอิง offset สัมบูรณ์

---

## 8. สรุปสิ่งที่ต้องทำต่อ (ตามลำดับ ก่อนเริ่มแปลจริง)

1. **Implement decoder โหมด `0x01`** (LZSS เฉพาะเกม) แล้ว verify round-trip กับ ROM เหมือนที่ทำกับ LZ77 มาตรฐานแล้ว
2. **หาตำแหน่งฟอนต์จริง** โดยไล่โค้ด ARM แทนการเดาขนาดไฟล์ (ดูข้อ 5)
3. **เดิน block walker เป็นระบบ** เพื่อ dump corpus JSON เต็มรูปแบบ พร้อม raw-byte coverage check (ตาม skill `game-text-recon` ขั้นตอน 3–6) — ตอนนี้ทำได้แค่ขั้น "พิสูจน์ว่า codec ถูกต้อง" (ข้อ 3) ซึ่งเป็นพื้นฐานสำคัญที่สุดแล้ว
4. แยกว่าข้อความไหนเป็น **ข้อความจริง (แก้ไขได้)** กับ **ข้อความที่ฝังในกราฟิก** (เช่น โลโก้ชื่อเกม, ป้ายเมนูบางจุด) — ยังไม่ได้ตรวจในรอบนี้
5. เมื่อมี corpus + font แล้ว ค่อยเข้าสู่ขั้นแปลจริงด้วย skill `game-text-localize` และฝังฟอนต์ไทยด้วย `complex-script-game-font`

---

## 9. แหล่งอ้างอิง

- [Final Fantasy Tactics Advance — Data Crystal](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance)
- [Compression Formats](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance:Compression_Formats)
- [ROM Map](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance/ROM_map)
- [Strings](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance/Strings)
- [String Tables](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance/String_Tables)
- [Scripting](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance:Scripting)
- [Jobs](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance:Jobs) / [Items](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance:Items) / [Abilities](https://datacrystal.tcrf.net/wiki/Final_Fantasy_Tactics_Advance:Abilities)
- [FFHacktics Tools wiki](https://ffhacktics.com/wiki/Tools) (เครื่องมือชุมชนสำหรับแก้ข้อความ FFT/FFTA)
- [gba_dumper (GitHub)](https://github.com/attilathedud/gba_dumper) — ยูทิลิตี้ทั่วไปสำหรับดึงข้อความจาก GBA ROM
